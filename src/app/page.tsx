import Button from '@mui/material/Button';

const HomePage = () => {
  return (
    <div>
      <h1>Home page</h1>
      <Button variant="text">Text</Button>
    </div>
  );
};

export default HomePage;